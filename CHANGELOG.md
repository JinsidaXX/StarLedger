# 更新日志

本项目的所有重要变更都会记录在此文件中。

格式基于 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.0.0/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.3.0] - 2026-08-30

### 新增（桌面小组件）

- 4×2 小组件：本周期收入 / 支出 / 可用支出 / 结余
- 2×2 小组件：本周期收入 / 支出 / 可用支出
- 采用 androidx.glance（Compose 风格的 AppWidget DSL），与现有技术栈一致
- 深空主题与 App 视觉统一，点击跳回首页
- 记账保存/删除、周期结算后主动刷新小组件

### 新增（强制存储体系）

- 强制存储随主薪资下发时设置：不设置 / 固定金额 / 收入百分比
- 可用支出口径：收入 − 强制存储 − 非医疗支出（可为负）
- 结余 = 强制存储实际 + 可用支出
- 超支侵蚀强制存储，最多侵蚀到原定目标的 90%（保底 10%）
- 医疗类支出不占可用支出额度、任意金额不触发超支提示
- 单笔消费 > 50 元且动到强制存储时弹超支提示

### 变更

- 模板分配改用「可用资金（收入 − 强制存储）」作为分配基数
- 主页未分配信封时，可放心使用回退到可用支出口径
- 默认「医疗与应急」分类自动标记医疗豁免
- 小组件配色：收入红色、支出绿色、可用支出蓝色（两个小组件同步）
- 2×2 / 4×2 小组件内容垂直均匀分布，铺满卡片

### 数据库

- `budget_cycles` 新增 `forcedSavingType` / `forcedSavingValue` / `forcedSavingAmount`
- `categories` 新增 `isMedical`
- 数据库版本 4 → 5 → 6，保留数据迁移（非破坏性）

### 测试

- `ForcedSavingCalculatorTest`：固定金额 / 百分比 / 超支侵蚀 / 保底等 5 项单元测试

## [0.2.0] - 2026-08-30

### 新增（财务周期系统）

- 两套财务周期模式可切换：日历月（传统）/ 滚动薪资（薪资事件驱动）
- 收入区分 4 种类型：主薪资、红包转账、兼职收入、其他收入
- 主薪资录入确认弹窗：检测到主薪资时询问是否结束旧周期、开启新周期（展示已运行天数）
- 滚动薪资周期：不由日期自动闭合，最大运行天数仅告警（默认 50 天）
- 预算延迟生效时间（`effect_start_time`）字段预留：适配预发下月工资场景
- 手动结束并结算周期：二次确认后结算，结余滚入心愿存钱罐（接口预留）
- 周期关闭类型审计：确认新薪资 / 手动结束 / 日历自动闭合
- 约束：同一时间仅一个运行周期；滚动模式下消费须归属运行周期

### 数据库

- `transactions` 新增 `incomeType`
- `budget_cycles` 新增 `cycleMode` / `closeReason` / `maxRunDays` / `effectStartTime`
- 数据库版本 3 → 4，保留数据迁移（非破坏性）

### 测试

- `CycleServiceTest`：运行天数、最大天数告警、结算、主薪资判定等 7 项单元测试

## [0.1.0] - 2026-08-14

### 新增（MVP 第一版）

- 日常记账：支出、收入、转账，5 秒快速记账
- 账户管理：默认账户模板、总资产统计、负债账户
- 分类管理：默认学生分类、自定义图标与颜色
- 生活费分配：大学生默认模板、固定金额/比例/剩余分配
- 预算信封：各分类预算、实际支出与剩余实时更新
- 可放心使用金额：首页核心指标
- 大额消费计划：冷静期（7/14/30 天，可关闭）、延期、放弃、更换方案、购买关联账目
- 星图系统：月度恒星（星核/星芒/星环/星雾/彗星/光点）、年度星座
- 月末复盘：总结、预算偏差、结余处理（结转下期/归入缓冲/保留）
- 备份：JSON 导入导出、CSV 导出
- 双语界面：简体中文 / English，可跟随系统或手动切换
- 隐私：完全本地运行，无账号、无网络请求、无广告

### 未纳入 MVP（按规划延后）

- CSV 导入、周期账单、桌面小组件（V1.1）
- 高价值物品管理（V1.2）
- 可选 AI 分析（V1.3）
- F-Droid 发布、更多语言、平板布局（V1.4）
